; (The [www] header tells PHP-FPM to merge these settings into the existing default pool rather than trying to spawn a brand new separate pool.)
[www]
pm = dynamic
pm.max_children = 30
pm.start_servers = 8
pm.min_spare_servers = 4
pm.max_spare_servers = 16
pm.max_requests = 500